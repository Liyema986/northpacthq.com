# NorthPact documentation (canonical)

This folder is the **final, repo-level** home for product requirements.

- **PRD:** [`prd/`](./prd/) — full Product Requirements Document set (markdown).

v1 and v2 apps live in `../northpact-v1/` and `../northpact-v2/`; both implementations align to these PRDs.
