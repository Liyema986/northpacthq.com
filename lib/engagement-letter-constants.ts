import type { Id } from "@/convex/_generated/dataModel";

export type EngagementLetterVersionId = Id<"engagementLetterVersions">;

/** Template row that should receive the bundled AFS compilation letter from the server when still empty. */
export const AFS_BUNDLED_SYNC_VERSION_ID =
  "kn78q9686nj6nxwcc91fmffqz983kfc3" as EngagementLetterVersionId;
