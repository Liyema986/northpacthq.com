/**
 * Firm admin UI (owner / admin roles). Kept under `/super/...` so the URL path
 * does not collide with the database role name `admin` or wording like “administrator”.
 */
export const ADMIN_PANEL_PATH = "/super/administrator" as const;
